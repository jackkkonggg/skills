{
  "$schema": "https://opencode.ai/config.json",
  "instructions": ["~/.codex/AGENTS.md"],
  "mcp": {
    "motion": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "https://api.motion.dev/registry.tgz?package=motion-studio-mcp&version=6.2.0"
      ],
      "environment": {
        "TOKEN": "${MOTION_TOKEN}"
      }
    }
  }
}
